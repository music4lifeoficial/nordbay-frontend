"use client";
import { useToast } from '@/hooks/use-toast';

export default function ToastViewport() {
  const { toast } = useToast();
  // Placeholder viewport using sonner/toast notifications; no custom list maintained.
  return null;
}
