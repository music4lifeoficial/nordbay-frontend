// Legacy ESLint config removed in favor of flat config (eslint.config.js).
// Keeping a minimal export to prevent older tools from failing, but it does nothing.
module.exports = {
  root: true,
  overrides: [],
};
